@model IEnumerable<ProjectSem03.Models.CombineModels>

@{
    ViewData["Title"] = "UpcomingIndex";
}

<h2>Upcoming Exhibitions</h2>
<br />
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>ExhibitionName</th>
            <th>Place</th>
            <th>StartDate</th>
            <th>EndDate</th>
            <th>StaffName</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Exhibitions.ExhibitionId</td>
                <td>@item.Exhibitions.ExhibitionName</td>
                <td>@item.Exhibitions.Place</td>
                <td>@item.Exhibitions.StartDate</td>
                <td>@item.Exhibitions.EndDate</td>
                <td>@item.Staffs.StaffName</td>
                <td>
                    @Html.ActionLink("Update", "EditUpComing", new { id = item.Exhibitions.ExhibitionId })
                </td>
            </tr>
        }
    </tbody>
</table>

